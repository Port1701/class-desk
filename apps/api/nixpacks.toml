[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci", "npx playwright install --with-deps chromium"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/src/index.js"
