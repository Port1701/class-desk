#!/usr/bin/env sh
# Pre-commit hook: Check code quality before committing
# Order: type checking → building → formatting → linting
# This ensures errors are caught early before any auto-fixes are applied

npm run typecheck && npm run build && npm run format && npm run lint
